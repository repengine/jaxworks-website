{
  "name": "jaxworks-website",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "npm run build && npx wrangler dev",
    "deploy": "npm run build && npx wrangler deploy",
    "upload-version": "npm run build && npx wrangler versions upload"
  },
  "devDependencies": {
    "wrangler": "^4.105.0"
  }
}
